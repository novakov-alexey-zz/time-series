// SBT project does not really make sense, since the solution is just one script with a couple of Models
// entire program could be wrapped for example into Ammonite script

name := "time-series"

version := "0.1"

scalaVersion := "2.12.7"